generate a step-by-step instrument doc for:

- create a docker instance of linux on macOS.

- the linux should be suitable for installing a full function openclaw

- install openclaw on the docker linux

- the openclaw should be able to read and write a macOS shared folder.

<Instruments here>
