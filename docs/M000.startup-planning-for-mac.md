- Create a docker container on my macbook
- Install Openclaw in that container
  - Allocate proper disk size. If it is possible, the disk size should be dynamic.
- Provide a local AI service by Ollama
- Provide a local File server service for the Openclaw instance for later use
  - Local HTTP server or Local FTP server for later use
- Connect to the Openclaw and set it up
- Test:
  - Try to ask the Openclaw instance to recognize a picture
    - the picture is provided by the local File server service
    - the AI engine is provided by the local AI service
 |